const getDb = require('../util/database').getDb;

class TripType {
    constructor(options) {
        this._id = options._id
        this.name = this.name
        //this.coordinates = 
    }
}

module.exports = Trip